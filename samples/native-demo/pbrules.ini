# Example pb_code rule configuration. Pass with `--rules samples/native-demo/pbrules.ini`.
# Section names are Finding ids; rules not listed keep their built-in severity.

[code.endl_flush]
severity = none           ; this demo project doesn't care about std::endl

[code.tech_debt_markers]
severity = high           ; promote TODO/FIXME tracking for this project
